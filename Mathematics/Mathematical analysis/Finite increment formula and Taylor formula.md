# Finite increment formulla
Assume that $D$ is a open set in $\mathbb{R}^m$, $a = (a_1, ..., a_m)$ and $a + h = (a_1 + h_1, ..., a_m+h_m)$
are two points in $\bar{D}$, and $\bar{D}$ includes at least a open line which connect with these two points.
$$(a, a + h) \subset \bar{D}$$
The function $f(x)=f(x_1, ..., x_m)$
